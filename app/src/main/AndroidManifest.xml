<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="projetotcc.com.br.aventura">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.AventuraApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".AventuraDetalheActivity" />
        <activity
            android:name=".NovaAventuraActivity"
            android:label="Uma Nova Aventura" />
        <activity
            android:name=".MinhaAvaliacaoActivity"
            android:label="Minhas Avaliações" />
        <activity
            android:name=".AvaliarActivity"
            android:label="Avaliar"/>
    </application>

</manifest>